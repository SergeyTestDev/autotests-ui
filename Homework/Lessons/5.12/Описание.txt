В данном задании закрепим работу с CSS локаторами. Для этого вам предлагается написать правильные CSS локаторы в поле CSS. 
Сейчас для каждого стиля указан локатор wrong.style, что не верно, необходимо вместо неправильного локатора написать корректный

Важно:

    В поле HTML ничего изменять/редактировать не нужно
    Работать нужно только с полем CSS
    Вместо неправильных локаторов wrong.style необходимо написать корректные локаторы, чтобы выполнились условия

Для заголовка h3 должен примениться стиль font-size со значением 20px +
Для всех полей ввода input должен примениться стиль background-color со значением green
Для кнопки button должны примениться стили width, height со значениями 100px
Для всех элементов на странице должен примениться стиль display со значением flex
Для всех лейблов label должен примениться стиль background-color со значением green 